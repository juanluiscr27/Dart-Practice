// Logical operators. To create boolean expressions we use the logical operators.
void main() {
  // logical OR ( || )

  // logical AND ( &&	)

  // Not ( !expr )inverts the following expression (changes false to true, and vice versa)
}
