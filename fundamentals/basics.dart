// To import the core package from dart
import 'dart:core';

/// The main function is the program entry point of execution
void main() {
  // Type inference (dynamically type)
  var firstName = 'Juan Luis';

  // Statically typed variable
  String lastName = 'Casanova';

  // Print to the console
  print(firstName + ' ' + lastName);

  // Dynamic vs var

  // Type Safety
  // Soundness
  // Null Safety
}
